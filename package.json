{
  "name": "mackerel",
  "version": "0.1.0",
  "private": true,
  "description": "A protocol for malleable data querying in LLM-driven interfaces",
  "workspaces": [
    "packages/*",
    "example-site"
  ],
  "scripts": {
    "dev": "pnpm --filter example-site dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}

